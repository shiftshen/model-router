# Model Router 3.2.3 · Coding Plan 模型目录与配置同步修复

- 启动模型不再强制请求 /models。部分 Coding Plan 接口不提供模型目录，但推理接口正常；目录缺失不应阻止启动。
- 发现模型保留当前手动输入的别名。目录仅作候选，仍支持在编辑配置中手动输入模型 ID。
- 保存模型后同步已存在且未运行的专用/续接窗口配置和目录，避免模型库是新 ID、窗口仍是旧 ID。正在运行的专用窗口明确提示重开。
- 新增目录 401/404、手动别名、配置同步及运行窗口保护测试。

已通过真实 Codex CLI 对 ark-code-latest 执行 Shell 写入并读取测试文件。未修改用户供应商 Key，不能把某个目录或模型的认证报错推断为整份 Key 失效。

Windows 安装包仍为 Preview；双平台 CI 不替代 Windows 实机 GUI 验收。
